# Copy to .env and set a strong secret in production
JWT_SECRET=change-me-in-production
CHANNEL_NAME=mychannel
CHAINCODE_NAME=basic
# Leave false for CA-registered users (test network); set true only if identities have discovery ACLs
FABRIC_DISCOVERY=false
